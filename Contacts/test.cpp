/*#include <iostream>
#include <fstream>
using namespace std;

int main() {
	fstream fs;
	fs.open("contacts.bin", ios::ate | ios::out);
	if (fs.fail())
		cout << "failed" << endl;
    else
        cout << "not failed" << endl;
    fs.close();
	return 0;
}*/